app:
    version: '1조 - 0.1.1-SNAPSHOT'

spring:
    application:
        name: lotteon
    jpa:
        properties:
            hibernate:
                default_batch_fetch_size: 100
    profiles:
        active: dev
        include: local
    thymeleaf:
        cache: false
        prefix: classpath:/templates/
    mail:
        host: smtp.gmail.com
        port: 587
        username: dlehdud224@gmail.com
        password: xjoo ebnq ppso nxst
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
    servlet:
        multipart:
            enabled: true
            max-file-size: 30MB
            max-request-size: 30MB